#include <stdio.h>

/**
 * main - A c program that prints exactly the word.
 *
 * Return: returns nothing
 */
int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,");
	return (0);
}
